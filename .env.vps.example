# Copy to .env on the VPS before running docker-compose.vps.yml
# Generate strong values; do not keep defaults in public deployment.
MYSQL_ROOT_PASSWORD=change-this-long-random-password
INTERNAL_API_TOKEN=change-this-long-random-internal-token
